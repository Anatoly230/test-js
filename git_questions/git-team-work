fork - вилка, развилка.
Копирую проект, в котором работаю с чужого репозитория на свой, нажав кнопку fork (вилка). 
У меня в рпеозитории создаётся копия проекта, связанная с репозиторием, откуда я его скопировал.
Перехожу в этот проект у себя в репозитории (в браузере), копирую ссылку для клонирования, использую её с командой  git clone в терминале/консоле 
git clone git@github.com:Your_login/your-project.git

далее необходимо проверить связь моего удалёного репозитория, с локальным командой 
git remote -v

должно выйти сообщение : 
origin  https://github.com/Your_login/your-project.git (fetch) получать
origin  https://github.com/Your_login/your-project.git (push) отправлять


pull request -  запрос на слияние измениений в  проектах (запрос на слияние)

после внесенных изменией на локальном репозитории, их необходимо передать сначала на свой удалённый репозиторий,
 а потом на тот, откуда был произведён fork

 git push -u origin branch_name  
 /*-u   -   upstream : вверх по течению*/

 далее перехожу в проект на репозиторий в браузере, нажимаю кнопку pull request

 в появившихся полях записываю что и зачем/как было изменено (необходимо для истории и пониманию течения процесса)

 нажимаю create pull request

 если в чужом проекте произошли изменения, их необходимо подтянуть, снова сделать  fork.
 Можно сделать кнопкой на сайте, а можно через консоль (этот вариант более профессиональный).
 
  git remote add otherLoginName git@github.com:otherLoginName/otherProject.git 
 
 /*
 пояснения
 
 remote -   удалённый доступ
 add    -   добавить
 otherLoginName -   имя пользователя (можно выбрать любое котороткое)
git@github.com:otherLoginName/otherProject.git  -   копируется из clone with https  проекта 
(в ссылке ничего не меняется, копируется как есть, иначе нет смыла)
 */

далее перехожу на ветку master  своего проекта на своём репозитории

git checkout master

и забираю изменения в репозитории источнике (чужой репозиторий)

git pull otherLoginName master

все изменения произошедшие в проекте источнике (чужой репозиторий) перекачены на локальный репозиторий (в компьютере) в втеку master

теперь эти изменения можно перекинуть на удалённый на свой удалённый репозиторий 

git push origin master

